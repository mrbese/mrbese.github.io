---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectItem from '../../components/ProjectItem.astro';
import { projects } from '../../data/projects.ts';
---
<BaseLayout
  title="Projects · Omer Bese"
  description="Live energy and consumer software, from national rebate-program design to insurance-policy investigation."
  path="/projects"
>
  <div class="phead reveal">
    <div class="eyebrow">projects</div>
    <h1>Systems I shipped<span class="d">.</span></h1>
    <p class="sub">Built to answer a question I actually had. Public documentation where code stays private, open source where the code is ready.</p>
  </div>

  <section class="list">
    {projects.map((p) => <ProjectItem {...p} />)}
  </section>
</BaseLayout>
<style>
  .list{margin-top:56px}
</style>
